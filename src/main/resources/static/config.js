window.__QUIZ_PLATFORM_CONFIG__ = window.__QUIZ_PLATFORM_CONFIG__ || {
  // Leave empty to use same-origin API calls (default).
  // Set to your backend URL when frontend is hosted separately.
  apiBaseUrl: "",
};
